[package]
name = "crumble-tx"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
bincode = {version = "=2.0.1", features = ["serde"]}
tempfile = "3.27.0"
